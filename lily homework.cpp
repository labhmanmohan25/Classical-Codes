#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <assert.h>
#include <bits/stdc++.h> 
#include <vector>
using namespace std;

int main() {
	int n,i,j,cnt1,cnt2;
	cin>>n;
	long long int a[n],b;
	for(i=0;i<n;i++){
		cin>>a[i];
		b[i]=a[i];
	}
	sort(b, b+n);
	for(i=0;i<n;i++){
		
	}
	
	return 0;
}
